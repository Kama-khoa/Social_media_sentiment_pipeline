version: 2

sources:
  - name: raw
    database: "{{ env_var('GCP_PROJECT_ID') }}"
    schema: "{{ env_var('BQ_DATASET') }}"
    tables:
      - name: raw_videos
      - name: raw_comments
      - name: keyword_config
      - name: video_crawl_state
